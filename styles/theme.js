import { extendTheme } from "@chakra-ui/react";

export const defaultTheme = extendTheme({
    colors: {
        primary: "#1E212B",
        secondary: "#FCFBFC",
    },
    fonts: {
        body: "Montserrat",
        heading: "Montserrat",
    },
});
